{
  "variables": {
    "aws_access_key_id": "{{ env `AWS_ACCESS_KEY_ID` }}",
    "aws_secret_access_key": "{{ env `AWS_SECRET_ACCESS_KEY` }}",
    "region": "{{ env `AWS_REGION` }}",
    "environment": "{{ env `PACKER_ENVIRONMENT` }}"
  },
  "builders": [{
    "type": "amazon-ebs",
    "access_key": "{{ user `aws_access_key_id` }}",
    "secret_key": "{{ user `aws_secret_access_key` }}",
    "region": "{{user `region`}}",
    "spot_price_auto_product": "Linux/UNIX (Amazon VPC)",
    "ssh_pty": true,
    "instance_type": "t2.medium",
    "associate_public_ip_address": true,

    "source_ami_filter": {
      "filters": {
        "virtualization-type": "hvm",
        "name": "*RHEL-6.7_HVM_GA-*",
        "root-device-type": "ebs"
      },
      "owners": ["309956199498"],
      "most_recent": true
    },
    "ssh_username": "ec2-user",
    "ssh_timeout": "5m",

    "ami_virtualization_type": "hvm",
    "ami_description": "Consul Server",
    "ami_name": "se-consul-0.7.5-RHEL6.7-{{isotime \"2006-01-02-1504\"}}",
    "tags": {
      "Name": "{{user `environment`}} Consul Server",
      "System": "Consul OSS",
      "Component": "Consul",
      "OS": "RHEL-6.7"
    }
  }],
  "provisioners": [
  {
    "type": "shell",
    "start_retry_timeout": "10m",
    "inline": [
      "touch /tmp/provisioned"
    ]
  },
  {
    "type": "shell",
    "start_retry_timeout": "10m",
    "inline": [
      "sudo yum -y install curl",
      "sudo yum -y update nss openssl",
      "curl https://raw.githubusercontent.com/ncorrare/terraform-examples/master/provision.sh | /usr/bin/sudo /bin/bash",
      "/usr/bin/sudo /opt/puppetlabs/bin/puppet apply --environment packer -e 'include profile::consulserver'"
    ]
  },
  {
    "type": "shell",
    "start_retry_timeout": "5m",
    "inline": [
                        "sudo yum -y remove puppet-agent puppetlabs-release-pc1",
      "sudo rm -rf /etc/puppetlabs",
      "sudo rm -rf /var/lib/cloud/instances/*",
      "sudo rm -f /root/.ssh/authorized_keys",
      "sudo rm -f /home/ec2-user/.ssh/authorized_keys",
      "history -cw"
    ]
  }
  ]
}
